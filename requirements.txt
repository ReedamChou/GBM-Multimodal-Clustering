# ==============================================================================
# GBM Multi-Modal Clustering Pipeline — Requirements
# ==============================================================================
# Versions are pinned for Docker reproducibility.
# For local development, you can relax pins to >= if needed.
# ==============================================================================

# Core scientific computing
numpy==1.26.4
pandas==2.2.1
scipy==1.12.0
scikit-learn==1.4.1

# Medical imaging
nibabel==5.2.1
nilearn==0.10.4
antspyx==0.4.2

# Radiomics (pulls SimpleITK as dependency)
pyradiomics==3.1.0

# Survival analysis
lifelines==0.29.0

# Visualization
matplotlib==3.8.3
seaborn==0.13.2
umap-learn==0.5.5

# Explainability
shap==0.45.0

# Clustering extras
scikit-learn-extra==0.3.0

# Deep learning (CUDA 12.1 — must match Dockerfile base image)
# NOTE: When installing inside the Docker image (pytorch base), torch is
# already provided. This line is for local/conda installs.
torch==2.2.0

# Utilities
joblib==1.3.2
tqdm==4.66.2
pyyaml==6.0.1
